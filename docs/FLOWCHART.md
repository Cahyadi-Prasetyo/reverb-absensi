# 🔄 System Flowchart

**Laravel Reverb Absensi - Complete Flow Diagram**

---

## 📊 System Architecture Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT (Browser)                          │
│  - Vue 3 Components                                          │
│  - Inertia.js (SPA-like experience)                         │
│  - Laravel Echo (WebSocket client)                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ HTTP/HTTPS
                     │ WebSocket (ws://)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              NGINX LOAD BALANCER (Port 80/443)              │
│  - Round-robin distribution                                  │
│  - SSL termination                                           │
│  - Static file serving                                       │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
   ┌────────┐  ┌────────┐  ┌────────┐
   │ App 1  │  │ App 2  │  │ App 3  │
   │ :9000  │  │ :9000  │  │ :9000  │
   └────┬───┘  └────┬───┘  └────┬───┘
        │            │            │
        └────────────┼────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
   ┌─────────┐              ┌─────────┐
   │  MySQL  │              │  Redis  │
   │  :3306  │              │  :6379  │
   └─────────┘              └────┬────┘
                                 │
                                 ▼
                          ┌─────────────┐
                          │   Reverb    │
                          │ WebSocket   │
                          │   :8080     │
                          └─────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Access /login
     ▼
┌──────────────┐
│ Login Page   │
│ (Fortify)    │
└────┬─────────┘
     │
     │ 2. Submit credentials
     ▼
┌──────────────────┐
│ Fortify Auth     │
│ - Validate       │
│ - Check 2FA      │
└────┬─────────────┘
     │
     ├─── ✅ Success
     │    │
     │    ▼
     │  ┌──────────────┐
     │  │ Create       │
     │  │ Session      │
     │  └────┬─────────┘
     │       │
     │       ▼
     │  ┌──────────────┐
     │  │ Redirect to  │
     │  │ Dashboard    │
     │  └──────────────┘
     │
     └─── ❌ Failed
          │
          ▼
     ┌──────────────┐
     │ Show Error   │
     │ Return Login │
     └──────────────┘
```

---

## 📝 Attendance Check-In Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Click "Check In"
     ▼
┌──────────────────────┐
│ AttendanceController │
│ - Validate user      │
│ - Check today record │
└────┬─────────────────┘
     │
     ├─── Already checked in?
     │    │
     │    ├─── Yes ❌
     │    │    │
     │    │    ▼
     │    │  ┌──────────────┐
     │    │  │ Return Error │
     │    │  └──────────────┘
     │    │
     │    └─── No ✅
     │         │
     │         ▼
     │    ┌──────────────────┐
     │    │ Create Record    │
     │    │ - check_in: now()│
     │    │ - status: auto   │
     │    │ - node_id        │
     │    └────┬─────────────┘
     │         │
     │         ▼
     │    ┌──────────────────┐
     │    │ Broadcast Event  │
     │    │ AttendanceCreated│
     │    └────┬─────────────┘
     │         │
     │         ▼
     │    ┌──────────────────┐
     │    │ Reverb WebSocket │
     │    │ - Channel:       │
     │    │   attendances    │
     │    └────┬─────────────┘
     │         │
     │         ▼
     │    ┌──────────────────┐
     │    │ All Connected    │
     │    │ Clients Receive  │
     │    │ Update           │
     │    └────┬─────────────┘
     │         │
     │         ▼
     │    ┌──────────────────┐
     │    │ Dashboard Auto   │
     │    │ Refreshes        │
     │    └──────────────────┘
     │
     └─── Return Success
```

---

## 🎛️ Admin Panel Flow

```
┌─────────┐
│  Admin  │
└────┬────┘
     │
     │ 1. Access /admin
     ▼
┌──────────────────┐
│ AdminMiddleware  │
│ - Check auth     │
│ - Check role     │
└────┬─────────────┘
     │
     ├─── role !== 'admin'? ❌
     │    │
     │    ▼
     │  ┌──────────────┐
     │  │ 403 Forbidden│
     │  └──────────────┘
     │
     └─── role === 'admin'? ✅
          │
          ▼
     ┌──────────────────┐
     │ Admin Dashboard  │
     │ - Load stats     │
     │ - Recent data    │
     └────┬─────────────┘
          │
          │ 2. Navigate to Users
          ▼
     ┌──────────────────┐
     │ Users Index      │
     │ - List users     │
     │ - Search/Filter  │
     │ - Pagination     │
     └────┬─────────────┘
          │
          ├─── 3a. Create User
          │    │
          │    ▼
          │  ┌──────────────┐
          │  │ Create Form  │
          │  └────┬─────────┘
          │       │
          │       ▼
          │  ┌──────────────┐
          │  │ Validate     │
          │  └────┬─────────┘
          │       │
          │       ▼
          │  ┌──────────────┐
          │  │ Store User   │
          │  └────┬─────────┘
          │       │
          │       ▼
          │  ┌──────────────┐
          │  │ Flash Success│
          │  │ Redirect     │
          │  └──────────────┘
          │
          ├─── 3b. Edit User
          │    │
          │    ▼
          │  ┌──────────────┐
          │  │ Edit Form    │
          │  │ (pre-filled) │
          │  └────┬─────────┘
          │       │
          │       ▼
          │  ┌──────────────┐
          │  │ Update User  │
          │  └────┬─────────┘
          │       │
          │       ▼
          │  ┌──────────────┐
          │  │ Flash Success│
          │  └──────────────┘
          │
          ├─── 3c. View User
          │    │
          │    ▼
          │  ┌──────────────┐
          │  │ Show Detail  │
          │  │ - User info  │
          │  │ - Statistics │
          │  │ - Attendances│
          │  └──────────────┘
          │
          └─── 3d. Delete User
               │
               ▼
          ┌──────────────┐
          │ Confirm?     │
          └────┬─────────┘
               │
               ├─── Yes ✅
               │    │
               │    ▼
               │  ┌──────────────┐
               │  │ Delete User  │
               │  └────┬─────────┘
               │       │
               │       ▼
               │  ┌──────────────┐
               │  │ Flash Success│
               │  └──────────────┘
               │
               └─── No ❌
                    │
                    ▼
               ┌──────────────┐
               │ Cancel       │
               └──────────────┘
```

---

## 🔄 Real-Time Update Flow

```
┌─────────────┐
│  User A     │
│ Check-in    │
└──────┬──────┘
       │
       │ 1. POST /attendances
       ▼
┌──────────────────┐
│ Controller       │
│ - Create record  │
└──────┬───────────┘
       │
       │ 2. Fire event
       ▼
┌──────────────────┐
│ AttendanceCreated│
│ Event            │
└──────┬───────────┘
       │
       │ 3. Broadcast
       ▼
┌──────────────────┐
│ Laravel Reverb   │
│ WebSocket Server │
└──────┬───────────┘
       │
       │ 4. Push to channels
       ├────────────────────┐
       │                    │
       ▼                    ▼
┌──────────────┐    ┌──────────────┐
│ User B       │    │ User C       │
│ Dashboard    │    │ Dashboard    │
└──────┬───────┘    └──────┬───────┘
       │                    │
       │ 5. Receive event   │
       ▼                    ▼
┌──────────────┐    ┌──────────────┐
│ Vue Component│    │ Vue Component│
│ Auto-refresh │    │ Auto-refresh │
└──────────────┘    └──────────────┘
```

---

## 📊 Data Flow Diagram

```
┌──────────────────────────────────────────────────────────┐
│                    REQUEST FLOW                           │
└──────────────────────────────────────────────────────────┘

Browser
   │
   │ 1. HTTP Request
   ▼
Nginx Load Balancer
   │
   │ 2. Forward to App
   ▼
Laravel App (1 of 3)
   │
   ├─── 3a. Read Data
   │    │
   │    ▼
   │  MySQL Database
   │    │
   │    └─── Return Data
   │
   ├─── 3b. Cache Check
   │    │
   │    ▼
   │  Redis Cache
   │    │
   │    ├─── Hit ✅
   │    │    └─── Return Cached
   │    │
   │    └─── Miss ❌
   │         └─── Query DB
   │              └─── Cache Result
   │
   └─── 4. Render Response
        │
        ▼
   Inertia.js Response
   (JSON + Component)
        │
        │ 5. Return to Browser
        ▼
   Vue 3 Renders UI

┌──────────────────────────────────────────────────────────┐
│                  REAL-TIME FLOW                           │
└──────────────────────────────────────────────────────────┘

Laravel App
   │
   │ 1. Event Fired
   ▼
Redis (Pub/Sub)
   │
   │ 2. Publish Event
   ▼
Laravel Reverb
   │
   │ 3. Broadcast to Clients
   ▼
Connected Browsers
   │
   │ 4. Laravel Echo Receives
   ▼
Vue Components Update
```

---

## 🔐 Security Flow

```
┌──────────────────────────────────────────────────────────┐
│                  MIDDLEWARE STACK                         │
└──────────────────────────────────────────────────────────┘

Request
   │
   ▼
┌──────────────┐
│ CSRF Check   │
│ - Verify     │
│   token      │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Auth Check   │
│ - Session    │
│   valid?     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Verified     │
│ - Email      │
│   verified?  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Admin Check  │
│ (if admin    │
│  route)      │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Controller   │
│ Action       │
└──────────────┘
```

---

## 📱 User Journey Map

```
┌─────────────────────────────────────────────────────────┐
│              EMPLOYEE DAILY FLOW                         │
└─────────────────────────────────────────────────────────┘

Morning:
  Login → Dashboard → Check-In → See Confirmation

During Day:
  View History → Check Status → See Statistics

Evening:
  Check-Out → View Duration → Logout

┌─────────────────────────────────────────────────────────┐
│              ADMIN DAILY FLOW                            │
└─────────────────────────────────────────────────────────┘

Morning:
  Login → Admin Dashboard → Review Today's Attendances

During Day:
  Manage Users → View Reports → Handle Issues

As Needed:
  Create User → Edit Records → Export Data
```

---

## 🎯 Decision Flow

```
┌──────────────────────────────────────────────────────────┐
│           ATTENDANCE STATUS DECISION                      │
└──────────────────────────────────────────────────────────┘

Check-In Time
   │
   ▼
Is time <= 09:00?
   │
   ├─── Yes ✅
   │    │
   │    ▼
   │  Status = "on_time"
   │
   └─── No ❌
        │
        ▼
   Status = "late"

┌──────────────────────────────────────────────────────────┐
│           ADMIN ACCESS DECISION                           │
└──────────────────────────────────────────────────────────┘

User tries /admin
   │
   ▼
Is authenticated?
   │
   ├─── No ❌ → Redirect to Login
   │
   └─── Yes ✅
        │
        ▼
   Is role === 'admin'?
        │
        ├─── No ❌ → 403 Forbidden
        │
        └─── Yes ✅ → Allow Access
```

---

## 🔄 Complete System Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    COMPLETE FLOW                             │
└─────────────────────────────────────────────────────────────┘

1. User Login
   └─→ Fortify Auth
       └─→ Session Created
           └─→ Redirect Dashboard

2. View Dashboard
   └─→ Load Stats (MySQL)
       └─→ Cache in Redis
           └─→ Render Vue Component

3. Check-In
   └─→ Validate Request
       └─→ Create Attendance (MySQL)
           └─→ Fire Event
               └─→ Broadcast (Reverb)
                   └─→ Update All Clients

4. Admin Access
   └─→ Check Middleware
       └─→ Load Admin Panel
           └─→ Manage Users
               └─→ CRUD Operations
                   └─→ Flash Messages

5. Real-Time Updates
   └─→ WebSocket Connection
       └─→ Listen Events
           └─→ Auto-Refresh UI
               └─→ No Page Reload
```

---

**This flowchart represents the actual implementation of the system.**
